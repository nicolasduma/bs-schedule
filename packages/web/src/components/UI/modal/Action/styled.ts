import tw, { styled } from 'twin.macro'

import { Button } from '../..'

export const NotButton = styled(Button.Filled)`
  ${tw`
  
  bg-darkslategray-600
  text-zinc-100

  hover:bg-darkslategray-500

  `}
`
